<UserControl
    x:Class="KillConfirmGameBar.Controls.Settings.Battlefield4AdvancedSettingsPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border
        x:Name="Card"
        Padding="22"
        Background="#14273A"
        BorderBrush="#4D9ED6"
        BorderThickness="1"
        CornerRadius="24">
        <StackPanel Spacing="10">
            <TextBlock
                x:Name="TitleText"
                FontSize="20"
                FontWeight="Bold"
                Text="Battlefield 4 advanced settings" />
            <TextBlock
                x:Name="BodyText"
                FontSize="13"
                LineHeight="18"
                Text="BF4 uses a separate Battlefield 4 HUD file set, bf4 sound pack, and BF4-only UI. Other game settings stay out of this module."
                TextWrapping="WrapWholeWords" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Border x:Name="AssetTag" Padding="8,4" BorderThickness="1" CornerRadius="12">
                    <TextBlock x:Name="AssetTagText" FontSize="11" FontWeight="SemiBold" Text="bf4 assets" />
                </Border>
                <Border x:Name="HudTag" Padding="8,4" BorderThickness="1" CornerRadius="12">
                    <TextBlock x:Name="HudTagText" FontSize="11" FontWeight="SemiBold" Text="Text HUD" />
                </Border>
                <Border x:Name="ImportLockTag" Padding="8,4" BorderThickness="1" CornerRadius="12">
                    <TextBlock x:Name="ImportLockTagText" FontSize="11" FontWeight="SemiBold" Text="No file import" />
                </Border>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
