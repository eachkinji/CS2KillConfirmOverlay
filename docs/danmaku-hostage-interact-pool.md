# 人质交互池编辑记录

当前十六个事件池均已精选为 100 条，共 1600 条；下文保留本池接入时的记录。

`Widget/Danmaku/EventPools/hostage_interact.json` 已从旧版1000条替换为精选 **100 条**。

## 2026-09-06 人质交互池精选为 100 条

直接使用已有 Gemini 稿件，本轮无新调用。汇总稿 28 条，另从旧 builder 的 AST 提取 110 条 tuple，不执行旧脚本。共 138 条输入，经来源、格式和去重检查留下 131 条候选。回读候选涉及的全部原库完整文本后选出 100 条，采用 22 条汇总稿和 78 条脚本稿；保留原始出处、改写说明，文本修订记录在 `host_revision`。审查由 agent 完成，不等于人类审定。

成品 99 条带 emoji，82 个不同来源，同源最多 3 条，57 条保留至少四字连续原句片段，97 条主要嘲讽意图，最长 29 个 UTF-16 字符。原句保留率不等于经典热度。与前十四池无归一化重文，池内近似句另做人工语义标准的 agent 复核。重点保留原库的公式化解说、没人质疑我吧、玩出你删游戏吧、引退宣言、万除不等式等结构，写人质求售后、救星抢功、绑匪交易和虚构小剧场。

`KillConfirmService/src/gsi/event_decisions.rs` 在人质地图、有效回合阶段且没有同期击杀或助攻时，以未分配金钱增量调用 `economy/rules.rs::hostage_objective_kind`。非救援回合的支持奖励映射为 `hostage_interact`；这不保证已经背起人质、撤离成功、特定路线、耗时或受伤。成品删除固定按键和硬编事件事实，不写固定奖励；人质对白、救援评价和交易为拟人化反串。原始摘录不随成品称呼修改。

当前十五池各 100 条，人质救出池仍 1000 条，总计 2500 条。下一池 `hostage_rescue`。本轮记录与备份位于 `.agy-staff/hostage-interact-curation/`。未改分类器、最低加载数量、随机抽取及近期 64 条排重，未打包安装。

验证：`Test-DanmakuEventPools.ps1` 与 `Test-DanmakuEngine.ps1` 均退出 0；本轮变更 `git diff --check` 通过，原库、其他十五个事件池与两个生命周期池共 18 个文件哈希未变。
