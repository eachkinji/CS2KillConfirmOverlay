<UserControl
    x:Class="KillConfirmGameBar.Controls.GameStyles.CrosshairOffsetEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///Pages/KillConfirmWidget/KillConfirmWidgetPage.Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel HorizontalAlignment="Stretch" Spacing="6">
        <Border x:Name="Divider" Height="1" />

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="TitleLabel"
                VerticalAlignment="Center"
                FontSize="10"
                FontWeight="SemiBold"
                Text="准心中心微调" />
            <TextBlock
                x:Name="CurrentValueText"
                Grid.Column="1"
                VerticalAlignment="Center"
                FontSize="9"
                FontWeight="SemiBold"
                Text="X 0 · Y 0" />
        </Grid>

        <Grid x:Name="ResponsiveSettingsGrid" ColumnSpacing="18" RowSpacing="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="ManualColumn" Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel x:Name="PresetPanel" Spacing="6">
                <TextBlock
                    x:Name="QuickPresetLabel"
                    FontSize="9"
                    Text="快速方案" />
                <Grid ColumnSpacing="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button
                        x:Name="DefaultPresetButton"
                        MinWidth="0"
                        Height="28"
                        Padding="3,0"
                        HorizontalAlignment="Stretch"
                        Click="OnPresetClick"
                        FontSize="9"
                        Style="{StaticResource FluentSecondaryButtonStyle}"
                        Tag="0|0" />
                    <Button
                        x:Name="Preset1Button"
                        Grid.Column="1"
                        MinWidth="0"
                        Height="28"
                        Padding="3,0"
                        HorizontalAlignment="Stretch"
                        Click="OnPresetClick"
                        FontSize="9"
                        Style="{StaticResource FluentSecondaryButtonStyle}"
                        Tag="0.25|0.25" />
                    <Button
                        x:Name="Preset2Button"
                        Grid.Column="2"
                        MinWidth="0"
                        Height="28"
                        Padding="3,0"
                        HorizontalAlignment="Stretch"
                        Click="OnPresetClick"
                        FontSize="9"
                        Style="{StaticResource FluentSecondaryButtonStyle}"
                        Tag="0.25|0.5" />
                    <Button
                        x:Name="Preset3Button"
                        Grid.Column="3"
                        MinWidth="0"
                        Height="28"
                        Padding="3,0"
                        HorizontalAlignment="Stretch"
                        Click="OnPresetClick"
                        FontSize="9"
                        Style="{StaticResource FluentSecondaryButtonStyle}"
                        Tag="0.5|0.25" />
                    <Button
                        x:Name="Preset4Button"
                        Grid.Column="4"
                        MinWidth="0"
                        Height="28"
                        Padding="3,0"
                        HorizontalAlignment="Stretch"
                        Click="OnPresetClick"
                        FontSize="9"
                        Style="{StaticResource FluentSecondaryButtonStyle}"
                        Tag="0.5|0.5" />
                </Grid>
            </StackPanel>

            <StackPanel x:Name="ManualPanel" Grid.Column="1" Spacing="6">
                <TextBlock
                    x:Name="ManualInputLabel"
                    FontSize="9"
                    Text="手动输入" />
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="2">
                        <TextBlock
                            x:Name="OffsetXLabel"
                            FontSize="9"
                            Text="X 右移 (px)" />
                        <TextBox
                            x:Name="OffsetXBox"
                            KeyDown="OnOffsetKeyDown"
                            MaxLength="8"
                            LostFocus="OnOffsetXLostFocus"
                            PlaceholderText="0"
                            Style="{StaticResource FluentInputStyle}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Spacing="2">
                        <TextBlock
                            x:Name="OffsetYLabel"
                            FontSize="9"
                            Text="Y 下移 (px)" />
                        <TextBox
                            x:Name="OffsetYBox"
                            KeyDown="OnOffsetKeyDown"
                            MaxLength="8"
                            LostFocus="OnOffsetYLostFocus"
                            PlaceholderText="0"
                            Style="{StaticResource FluentInputStyle}" />
                    </StackPanel>
                </Grid>
                <TextBlock
                    x:Name="OffsetHint"
                    FontSize="9"
                    Text="正值向右/下，负值向左/上；可输入小数。"
                    TextWrapping="WrapWholeWords" />
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
