# 下包池编辑记录

当前十六个事件池均已精选为 100 条，共 1600 条；下文保留本池接入时的记录。

`Widget/Danmaku/EventPools/bomb_plant.json` 已替换为精选 **100 条**，由既有及本轮 Gemini 稿件筛选。

## 2026-09-06 下包池精选为 100 条

沿用已有 25 条 Gemini 候选，本轮用户确认额度恢复后启动 20 个 high-effort Gemini 任务，每个 8 条；最终均有 JSON 落盘，共 160 条新稿。部分任务/收集器因 `state.json.writer-lock` 的 EEXIST 错误结束，未追加调用；已落盘稿件逐条独立审查，相关 `generation_warning` 和 job id 保留，不将任务退出码当作内容合格证明。

筛选时先到的 152 条新稿加旧稿共 177 条，基础校验均通过；初选 120 条逐条回读完整原文，剔除 20 条来源牵强、语句拗口、场景不符或同义重复的稿件。迟到的 `plant_retry_08.json` 另 8 条归档，未加入本轮名单，保留候选编号稳定。成品为 20 条旧稿、80 条本轮新稿，100 条均带 emoji，94 个不同原库来源，同源最多 2 条，92 条保留至少四字连续原文片段，100 条为主要嘲讽意图，最长 25 个 UTF-16 字符。原句保留率不代表经典热度。

重点是下包打工、混奖励、抢功、装懂和嘴硬；保留玩机包、公式化解说、偷鸡蛋、教学登山、安装包、大面包与原库反串。正文不使用“主播”。删除固定按键、错误姿势和假托其他选手现场发言的写法；修订记录于 `host_revision`，原文摘录保持原样。与前十二池无归一化重文，池内归一化相似度大于 0.65 的配对为零，并检查语义重复。审查由 agent 完成，不等于人类逐条审定。

`gsi/combat.rs` 的 `detect_bomb_planted_action` 通过 C4 消耗与已安放状态，或此前 bomb actor 与当前 steamid 匹配且状态转为 planted，判定下包完成。`event_decisions.rs` 发出 `bomb_plant`。该事件不证明爆炸、回合获胜、包点位置、队友掩护人数或固定键位。下包奖励按模式取值（休闲 200，其他 300），所以文案只说下包费或奖励，不写固定金额；载荷 `kill_count=0` 也不是玩家零杀。登山、保释金、食物和自动安装等属于反串比喻。

备份、提示词、任务收集、筛选、源文复核与检查点位于 `.agy-staff/bomb-plant-curation/`。当前十三池各 100 条，其余三池各 1000 条，共 4300 条。下一池为 `bomb_defuse`（拆包）。分类器、加载门槛和近期 64 条排重未改，未打包或安装。

本轮验证：`Test-DanmakuEventPools.ps1` 与 `Test-DanmakuEngine.ps1` 均通过（退出码 0）。
