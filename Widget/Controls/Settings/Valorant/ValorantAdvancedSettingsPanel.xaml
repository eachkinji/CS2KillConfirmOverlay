<UserControl
    x:Class="KillConfirmGameBar.Controls.Settings.ValorantAdvancedSettingsPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///Pages/KillConfirmWidget/KillConfirmWidgetPage.Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border
        x:Name="Card"
        Padding="18,16"
        Background="{StaticResource FluentCardBackground}"
        BorderBrush="{StaticResource FluentCardBorder}"
        BorderThickness="1"
        CornerRadius="8">
        <StackPanel Spacing="14">
            <StackPanel Spacing="4">
                <TextBlock
                    x:Name="TitleText"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Foreground="{StaticResource FluentTitleText}"
                    Text="Valorant 战斗设置" />
                <TextBlock
                    x:Name="BodyText"
                    FontSize="12"
                    LineHeight="16"
                    Foreground="{StaticResource FluentMutedText}"
                    Visibility="Collapsed"
                    TextWrapping="WrapWholeWords" />
            </StackPanel>

            <Border Style="{StaticResource CompactChoiceCardStyle}">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CompactCircleIconStyle}">
                        <FontIcon FontSize="13" Foreground="White" Glyph="&#xE9D9;" />
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                        <TextBlock x:Name="StreakModeLabel" Style="{StaticResource CompactChoiceLabelStyle}" Text="连杀计算规则" />
                        <TextBlock FontSize="12" Foreground="{StaticResource FluentMutedText}" Text="设置无畏契约击杀连杀窗口持续时间或按单回合生命周期计算。" />
                    </StackPanel>
                    <ComboBox x:Name="StreakModeSelector" Grid.Column="2" MinWidth="140" Style="{StaticResource CompactSettingsComboBoxStyle}" SelectionChanged="OnStreakModeSelectionChanged">
                        <ComboBoxItem x:Name="StreakLifeItem" Tag="life">单回合持续至死亡</ComboBoxItem>
                        <ComboBoxItem x:Name="StreakTimed5Item" Tag="timed_5">5秒连杀窗口</ComboBoxItem>
                        <ComboBoxItem x:Name="StreakTimed10Item" Tag="timed_10">10秒连杀窗口</ComboBoxItem>
                        <ComboBoxItem x:Name="StreakTimed15Item" Tag="timed_15">15秒连杀窗口</ComboBoxItem>
                    </ComboBox>
                </Grid>
            </Border>

            <Border Style="{StaticResource CompactChoiceCardStyle}">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CompactCircleIconStyle}">
                        <FontIcon FontSize="13" Foreground="White" Glyph="&#xE8B7;" />
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                        <TextBlock x:Name="ExternalAssetsLabel" Style="{StaticResource CompactChoiceLabelStyle}" Text="外部原生素材" />
                        <TextBlock x:Name="ExternalAssetsHint" FontSize="12" Foreground="{StaticResource FluentMutedText}" TextWrapping="WrapWholeWords" Text="视觉素材和音频会优先从外部目录加载；增删素材包后重启应用刷新列表。" />
                    </StackPanel>
                    <Button x:Name="OpenExternalAssetsButton" Grid.Column="2" Style="{StaticResource FluentActionBtnStyle}" Click="OnOpenExternalAssetsClicked">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon FontSize="12" Glyph="&#xE838;" />
                            <TextBlock x:Name="OpenExternalAssetsButtonText" Text="打开目录" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
