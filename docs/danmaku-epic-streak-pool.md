# 高连杀池编辑记录

当前十六个事件池均已精选为 100 条，共 1600 条；下文保留本池接入时的记录。

`Widget/Danmaku/EventPools/epic_streak.json` 已从旧版 1000 条替换为精选 **100 条**。只筛已有 Gemini 高连杀专稿，本轮未调用 Gemini。

例句：“偷吃锦鲤转运了？随缘操作也能连收🐟”“为什么要说两遍？第一遍狗运第二遍回音🗣️”“连杀靠蒙装逼靠吼，太有实力了🤡”。

重点保留运气透支、连杀后膨胀、观众反串拆台与原库梗结构。不是每句话都加“高连杀”前缀；真夸必须有句内反转，正文不使用“主播”泛称。引用的原文保持原样，agent 审查不等于人类逐条审定。

## 2026-09-06 高连杀池精选为 100 条

本轮未调用 Gemini。从三轮旧生成脚本中以 AST 读取字面量候选，没有执行旧脚本；共找到 535 条，基础校验保留 520 条。回读初选 114 条的完整原文，淘汰 14 条弱关联、重复或战况假设过多的内容，最终入选 100 条，全部来自高连杀专稿。

100 条均带 emoji，98 个原库来源，同源最多 2 条；74 条保留可核对的连续原句片段（不代表经典热度），85 条为主要嘲讽意图，最长 24 个 UTF-16 字符。与前六池无归一化重文；池内两两文本相似度大于 0.65 的配对为零，另做语义复核。

`ClassifyKill` 在末杀之后、刀杀/投掷物/爆头之前，按 `KillCount >= 5` 判定高连杀。因此保留“五个起步”的阈值表达，不将每次事件写死为五个人头、团灭或胜利；枪械专用的准星、盲射等描写改成通用操作。名字、emoji、标点、时间和等级限制假设同步修正，原文摘录不变。

两项弹幕回归通过，原库、其他 15 个事件池与两个生命周期池共 18 个文件哈希未变。当前七池各 100 条，其余九池各 1000 条，共 9700 条。下一池为 `last_kill`，尚未打包或安装。
