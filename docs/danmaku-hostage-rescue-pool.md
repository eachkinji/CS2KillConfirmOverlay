# 人质救出池编辑记录

`Widget/Danmaku/EventPools/hostage_rescue.json` 已从旧版1000条替换为精选 **100 条**。

## 2026-09-06 人质救出池精选为 100 条，十六池完成

先筛已有 Gemini 稿件，共 63 条；其中下班卸妆、狗运逃生模板较多，不足以选出 100 条。按用户既有要求启动 20 个 high-effort Gemini worker，每个 8 条，各自使用独立任务状态和输出文件。新稿共 160 条，20 个收集器均退出 0。合并输入 223 条，基础校验通过 223 条。最终选取旧稿 20 条、新稿 80 条；逐条回读所选来源完整原文，审查语义、来源联系、重复包袱和事件范围。文本修订保留 `host_revision`，原始来源摘录不改。审查由 agent 完成，不等于人类审定。

成品 99 条带 emoji，引用 100 个不同原库来源，同源最多 1 条，81 条保留至少四字连续原文，100 条为主要嘲讽意图，最长 25 个 UTF-16 字符。保留原文比例不等于经典热度。无池内或跨前十五池归一化重文；另审查近似句。方向为救出后的差评、改口、抢功拆台、售后与虚构人质感言，成品不使用“主播”。

`gsi/update.rs` 以最近 `round_wins` 结果为 `ct_win_rescue` 且玩家为 CT 标记救援回合；`gsi/event_decisions.rs` 在人质地图、有效回合阶段、没有同期击杀或助攻时，把个人未分配金钱增量交给 `economy/rules.rs::hostage_objective_kind`，匹配该模式奖励后归类 `hostage_rescue`。这是现有分类器的推断，不提供具体路线、用时、敌方枪法、受伤、按键，也不是整场比赛夺冠。文案不硬编这些事实，人质对白和售后等是拟人反串。

16 个事件池现在均为 100 条，总计 1600 条；原始库 23521 条保持不变。候选、提示词、worker 输出、收集日志、备份和检查点位于 `.agy-staff/hostage-rescue-curation/`。没有改分类器、运行时最低数量、随机抽取或近期 64 条排重；未打包安装。逐事件精简任务至此完成。

验证：`Test-DanmakuEventPools.ps1` 与 `Test-DanmakuEngine.ps1` 均退出 0；本轮变更 `git diff --check` 通过，原库、其他十五个事件池与两个生命周期池共 18 个文件哈希未变。
