# Overlay controls

Each reusable overlay belongs in its own feature folder. Keep the overlay XAML,
code-behind, and theme or behavior partials together.

- `Update`: update availability and download guidance overlay.
