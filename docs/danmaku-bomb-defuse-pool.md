# 拆包池编辑记录

当前十六个事件池均已精选为 100 条，共 1600 条；下文保留本池接入时的记录。

`Widget/Danmaku/EventPools/bomb_defuse.json` 已从旧版1000条替换为精选 **100 条**。

## 2026-09-06 拆包池精选为 100 条

已有 Gemini 专稿 28 条，本轮启动 20 个 high-effort Gemini worker，每个生成 8 条新稿，共 160 条。每个 worker 使用独立的任务状态文件，工作目录与权限规则不变；20 个收集器均退出 0，无上轮共享状态锁报错。候选合计 188 条，基础校验均通过。初选 112 条逐条回读完整原文，淘汰 12 条真夸未拆台、原梗转接牵强、场景错配或重复笑点的稿件，最终采用 20 条旧稿与 80 条新稿。

成品 100 条均带 emoji，95 个不同来源，同源最多 2 条，92 条保留至少四字连续原文片段，100 条为主要嘲讽意图，最长 26 个 UTF-16 字符。原句保留率不代表经典热度。与前十三池无归一化重文，池内归一化相似度大于 0.65 的配对为零，另做语义复核。

围绕拆包后的抢功、售后服务、钳子拟人、C4吐槽、马甲重复领功和嘴硬展开。成品不使用“主播”；原文摘录保留，固定按键、姿势、死包及真实发言设置等修订记录于 `host_revision`。候选与来源、提示词、各 worker 输出和检查点在 `.agy-staff/bomb-defuse-curation/`。审查由 agent 完成，不等于人类逐条审定。

`gsi/combat.rs` 的 `detect_bomb_defused_action` 要求炸弹状态转为 `defused`，且此前 bomb actor 与 steamid 相符，或玩家为 CT 且金钱增量等于当前模式拆包奖励；随后 `event_decisions.rs` 发送 `bomb_defuse`。它不提供剩余时间、拆弹器持有情况、烟雾、击杀数、敌方人数或键位，也不表示整场比赛获胜。奖励随模式变化，文案不写固定金额。虾钳、剪线、放贷、海鲜过敏和C4对白是比喻，不是额外检测事实。

当前十四池各 100 条，其余两池各 1000 条，共 3400 条。下一池为 `hostage_interact`（人质交互）。分类器、加载门槛和近期 64 条排重未改，未打包或安装。

本轮验证：`Test-DanmakuEventPools.ps1` 与 `Test-DanmakuEngine.ps1` 均通过（退出码 0）。
